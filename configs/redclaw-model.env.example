# Override the model served by redclaw-model.service.
REDCLAW_MODEL_ID=Qwen/Qwen2.5-7B-Instruct

# Bind the local model runtime to loopback by default.
REDCLAW_MODEL_HOST=127.0.0.1
REDCLAW_MODEL_PORT=8000
