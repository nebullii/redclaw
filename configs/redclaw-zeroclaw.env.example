# Optional extra environment overrides for redclaw-zeroclaw.service.
# Keep the assistant local-first and loopback-only by default.
#
# Gateway bind address is currently controlled by /etc/redclaw/config.toml:
# [gateway]
# host = "127.0.0.1"
# port = 42617
